<div class="wrap">
    <div class="container">

        {{>navbar}}
        <div class="page-content">
            <h1>{{pagetitle}}</h1>
            <div class="row">
                <div class="col-md-8 col-sm-12">
                    {{{content}}}
                </div>
            </div>
        </div>

    </div>
</div>
